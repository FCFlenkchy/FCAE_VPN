<p align="center">
  <img src="mountain.png" alt="FCAE VPN" width="100%">
</p>

# FCAE VPN

**[English](README.md)** | **فارسی** | **[中文](READMECH.md)**

یک ابزار دور زدن سانسور طراحی‌شده برای شبکه‌های به‌شدت محدود. این ابزار به‌طور خودکار مسیرهای قابل‌دسترس را کشف می‌کند، یک تونل رمزگذاری‌شده برقرار می‌کند و یک پروکسی محلی SOCKS5/HTTP برای برنامه‌های شما فراهم می‌کند.

ساخته‌شده بر پایه **[Aether](https://github.com/CluvexStudio/aether)** با رابط گرافیکی بومی برای ویندوز، لینوکس و اندروید.

## نحوه کار

FCAE VPN به **شبکه WARP کلادفلر** متصل می‌شود — همان زیرساختی که پشت سرویس DNS کلادفلر 1.1.1.1 است. روند کار به این صورت است:

1. **تهیه حساب** — در اولین اجرا، کلاینت یک هویت دستگاه WARP ایجاد می‌کند و آدرس‌های اختصاصی IPv4/IPv6 به همراه کلیدهای WireGuard از API ثبت‌نام کلادفلر دریافت می‌کند.
2. **اسکن نقاط اتصال** — کلاینت لیستی از آدرس‌های لبه کلادفلر را در چندین پورت بررسی می‌کند تا یک دروازه قابل‌دسترس پیدا کند. هر گزینه با یک هندشیک واقعی (و در حالت ironclad به‌صورت اختیاری با یک درخواست HTTP کامل) اعتبارسنجی می‌شود تا تأیید شود مسیر واقعاً ترافیک را عبور می‌دهد.
3. **برقراری تونل** — پس از پیدا شدن یک لبه کارآمد، یک تونل رمزگذاری‌شده باز می‌شود:
   - **MASQUE** — ترافیک در داخل نشست‌های HTTP/3 (QUIC) یا HTTP/2 (TLS) با استفاده از روش `CONNECT-IP` کپسوله می‌شود، که آن را برای سیستم‌های DPI شبیه ترافیک عادی HTTPS نشان می‌دهد.
   - **WireGuard** — یک تونل UDP استاندارد WireGuard مستقیماً به گره لبه برقرار می‌شود.
   - **WARP-in-WARP (gool)** — دو تونل WireGuard تو در تو برای لایه رمزگذاری اضافی.
4. **پروکسی محلی** — تونل یک پروکسی محلی SOCKS5 (پورت 1819) و HTTP (پورت 1820) در معرض نمایش قرار می‌دهد. برنامه‌هایی که برای استفاده از این پروکسی‌ها پیکربندی شده‌اند، ترافیک خود را از طریق تونل رمزگذاری‌شده به اینترنت از طریق شبکه کلادفلر عبور می‌دهند.

تمام ترافیک بین کلاینت و کلادفلر رمزگذاری شده است. از کلادفلر به بعد، ترافیق به‌طور عادی به اینترنت عمومی خارج می‌شود.

### نمودار معماری

```
┌─────────────────────────────────────────────────────────────────────┐
│                     برنامه شما                                       │
│              (مرورگر، اپلیکیشن یا ترافیک سیستم از طریق TUN)        │
└──────────────────────────────┬──────────────────────────────────────┘
                               │ SOCKS5 :1819 / HTTP :1820
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     کلاینت FCAE VPN                                 │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌──────────────┐   │
│  │  Netstack  │  │  اسکنر     │  │  رمزگذاری  │  │  مانیتور     │   │
│  │ (TCP/IP)   │  │ (کشف       │  │  (aether-  │  │  (اتصال مجدد │   │
│  │            │  │  نقاط اتصال)│  │   noize)   │  │   در خرابی)  │   │
│  └──────┬─────┘  └────────────┘  └────────────┘  └──────────────┘   │
│         │                                                           │
│         ▼                                                           │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                    تونل رمزگذاری‌شده                          │   │
│  │   ┌───────────┐   ┌──────────────┐   ┌──────────────────┐    │   │
│  │   │  MASQUE   │   │  WireGuard   │   │  WARP-in-WARP    │    │   │
│  │   │ HTTP/3/2  │   │   (UDP)      │   │  (WG داخل WG)    │    │   │
│  │   └─────┬─────┘   └──────┬───────┘   └────────┬─────────┘    │   │
│  └─────────┼────────────────┼────────────────────┼──────────────┘   │
└────────────┼────────────────┼────────────────────┼──────────────────┘
             │                │                    │
             ▼                ▼                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   لبه WARP کلادفلر                                  │
│          (162.159.192.x — کشف خودکار)                               │
└──────────────────────────────┬──────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       اینترنت عمومی                                  │
└─────────────────────────────────────────────────────────────────────┘
```

### مقایسه پروتکل‌ها

| پروتکل | حمل‌ونقل | مقاومت در برابر DPI | سرعت | مورد استفاده |
|----------|-----------|-------------------|-------|----------|
| **MASQUE (HTTP/3)** | QUIC روی UDP | بهترین — شبیه HTTPS | سریع | پیش‌فرض، بیشترین مقاومت در برابر سانسور |
| **MASQUE (HTTP/2)** | TLS روی TCP | بهترین — شبیه HTTPS | سریع | جایگزین وقتی QUIC مسدود شده |
| **WireGuard** | UDP | متوسط — رمزگذاری‌شده ولی قابل تشخیص | سریع‌ترین | وقتی UDP مجاز است |
| **WARP-in-WARP** | UDP تو در تو | بالا — رمزگذاری دوگانه | متوسط | لایه اضافی وقتی WG به تنهایی مسدود شده |

## امکانات

- کشف خودکار نقاط اتصال با اعتبارسنجی داده‌ای سرتاسری
- پشتیبانی از MASQUE (HTTP/3 QUIC / HTTP/2)، WireGuard و WARP-in-WARP (gool)
- رمزگذاری ترافیک با پروفایل‌های قابل پیکربندی
- اتصال مجدد خودکار با قابلیت اتصال سریع
- پروکسی‌های محلی SOCKS5 و HTTP
- رابط گرافیکی بومی روی تمام پلتفرم‌ها (ImGui + DirectX11/OpenGL روی دسکتاپ، Kotlin Material UI روی اندروید)

## پلتفرم‌ها

| پلتفرم | بک‌اند | رابط کاربری |
|----------|---------|----|
| ویندوز | DirectX 11 | ImGui |
| لینوکس | GLFW + OpenGL | ImGui |
| اندروید | Kotlin Material VpnService + پل JNI | Kotlin Material UI |

## ساخت

### پیش‌نیازها

- Rust (آخرین نسخه پایدار)
- کامپایلر C/C++ (GCC/Clang/MSVC)
- CMake >= 3.22
- Vulkan SDK یا DirectX SDK (ویندوز)
- برای اندروید: NDK، Android SDK، Kotlin

### ابتدا موتور Rust را بسازید

```bash
cargo build --manifest-path core/Cargo.toml -p aether-ffi --release
```

### ساخت رابط گرافیکی بومی

```bash
cmake -B build -DAETHER_TARGET=LINUX_X64
cmake --build build --config Release
```

 Targets: `LINUX_X64`, `WIN_X64`, `ANDROID_ARM64`.

### اندروید

پوشه `android/` را در Android Studio باز کنید و بسازید. تنظیمات Gradle به‌طور خودکار CMake را با `ANDROID_ARM64` فراخوانی می‌کند.

## قدردانی

- **[Aether](https://github.com/CluvexStudio/aether)** — موتور اصلی دور زدن سانسور توسط CluvexStudio. پروتکل‌های MASQUE، WireGuard و WARP-in-WARP را فراهم می‌کند.
- **[Dear ImGui](https://github.com/ocornut/imgui)** — کتابخانه رابط گرافیکی حالت فوری. برای تمام رندرهای بومی دسکتاپ استفاده می‌شود.
- **[Quiche](https://github.com/cloudflare/quiche)** — پیاده‌سازی HTTP/3 و QUIC کلادفلر. به‌عنوان بک‌اند حمل‌ونقل QUIC برای پشتیبانی از پروتکل MASQUE استفاده می‌شود.
- **[Wintun](https://www.wintun.net/)** — یک درایور TUN برای ویندوز ساخته‌شده توسط WireGuard. یک رابط شبکه با کارایی بالا در لایه ۳ برای تونل کردن ترافیک فراهم می‌کند.
- **[tun2socks](https://github.com/xjasonlyu/tun2socks)** — یک کتابخانه Go که ترافیک دستگاه TUN را به‌صورت شفاف از طریق پروکسی SOCKS5 مسیریابی می‌کند. قدرت‌دهنده حالت TUN VPN در سطح سیستم.

## مشارکت

مشارکت‌ها خوش‌آمد هستند! چه گزارش باگ، درخواست ویژگی، بهبود مستندات یا مشارکت کد — می‌توانید یک Issue یا Pull Request باز کنید.

### نحوه مشارکت

1. مخزن را Fork کنید
2. یک شاخه ویژگی بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات خود را Commit کنید (`git commit -m 'Add amazing feature'`)
4. به شاخه Push کنید (`git push origin feature/amazing-feature`)
5. یک Pull Request باز کنید

## مجوز

لطفاً مجوزهای هر یک از اجزا را در پروژه‌های مربوطه مشاهده کنید.

---

<div align="center">

### آیا این پروژه برایتان مفید بود؟

اگر این پروژه به شما در دور زدن سانسور کمک کرد یا وقتتان را ذخیره کرد، لطفاً یک **ستاره** بدهید — این کار به دیگران کمک می‌کند ابزار را پیدا کنند و انگیزه‌ای برای ادامه توسعه است.

[![Star](https://img.shields.io/github/stars/FCFlenkchy/FCAE_VPN?style=social)](https://github.com/FCFlenkchy/FCAE_VPN)

</div>
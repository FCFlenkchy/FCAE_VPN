[package]
name = "fcae-abi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Pure #[repr(C)] contract types for the FCAE VPN FFI. No runtime deps, no I/O."

[lib]
name = "fcae_abi"
path = "src/lib.rs"

# Deliberately dependency-free: this crate is the ABI contract and is the
# cbindgen input. Anything added here ends up in the generated C header.
[dependencies]

[build-dependencies]

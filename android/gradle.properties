android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Show every Gradle deprecation warning individually (instead of the one-line
# summary), so each one can be traced to our scripts vs. plugins and fixed or
# reported upstream.
org.gradle.warning.mode=all
